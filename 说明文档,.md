# 银河麒麟运维管理工具 - 架构与功能文档

## 一、项目概述

**银河麒麟运维管理工具**是一款专为银河麒麟桌面操作系统 V10 SP1 设计的系统运维管理工具。经过架构重构，现已整合为单一应用程序，无需额外部署服务端组件，可直接独立运行，提供系统信息监控、服务管理、网络配置、用户管理、日志清理等核心功能。

---

## 二、系统架构

### 2.1 架构设计

```
┌───────────────────────────────────────────────────────────┐
│                    整合版应用程序                          │
│                                                           │
│  ┌─────────────────┐    ┌─────────────────────────────┐   │
│  │   UI层 (PyQt5)  │───▶│      业务逻辑层            │   │
│  │  main_window.py │    │  core/commands.py          │   │
│  └─────────────────┘    │  core/local_client.py      │   │
│                         └───────────┬───────────────┘   │
│                                     │                     │
│                                     ▼                     │
│                         ┌─────────────────────────────┐   │
│                         │      系统资源层              │   │
│                         │  psutil / subprocess / os   │   │
│                         └─────────────────────────────┘   │
└───────────────────────────────────────────────────────────┘
```

### 2.2 模块划分

| 模块 | 职责 | 文件位置 |
| :--- | :--- | :--- |
| **core** | 核心业务逻辑 | [core/commands.py](file:///e:/Operation_Tools/core/commands.py), [core/local_client.py](file:///e:/Operation_Tools/core/local_client.py) |
| **ui** | 用户界面 | [ui/main_window.py](file:///e:/Operation_Tools/ui/main_window.py) |
| **入口** | 应用程序入口 | [kylintools.py](file:///e:/Operation_Tools/kylintools.py) |
| **server (保留)** | 旧版服务端代码 | [server/server.py](file:///e:/Operation_Tools/server/server.py) |
| **client (保留)** | 旧版客户端代码 | [client/client.py](file:///e:/Operation_Tools/client/client.py), [client/gui.py](file:///e:/Operation_Tools/client/gui.py) |

### 2.3 目录结构

```
Operation_Tools/
├── core/                    # 核心业务逻辑模块 (新增)
│   ├── __init__.py
│   ├── commands.py          # 系统命令实现
│   └── local_client.py      # 本地客户端代理
├── ui/                      # 用户界面模块 (新增)
│   ├── __init__.py
│   └── main_window.py       # 整合后的主窗口
├── server/                  # 服务端模块 (保留兼容)
│   ├── __init__.py
│   ├── __main__.py
│   ├── server.py
│   └── mock_server.py
├── client/                  # 客户端模块 (保留兼容)
│   ├── __init__.py
│   ├── __main__.py
│   ├── client.py
│   ├── gui.py
│   ├── enhanced_gui.py
│   └── apple_ui.py
├── debian/                  # DEB打包配置
│   └── kylin-system-tools/
├── kylintools.py            # 整合版主入口 (新增)
├── validate.py              # 功能验证脚本 (新增)
├── build-deb.sh             # 构建脚本
└── setup.py                 # Python包配置
```

---

## 三、核心功能模块

### 3.1 功能总览

| 功能模块 | 功能描述 | 对应方法 |
| :--- | :--- | :--- |
| 系统信息 | 主机名、内核版本、架构等 | `get_system_info()` |
| CPU监控 | CPU核心数、频率、使用率 | `get_cpu_info()` |
| 内存监控 | 内存总量、使用量、Swap | `get_memory_info()` |
| 磁盘监控 | 分区信息、磁盘使用率 | `get_disk_info()` |
| 网络管理 | 网络接口、IP地址、连接数 | `get_network_info()`, `restart_network()` |
| 进程管理 | 进程列表、CPU/内存占用 | `get_process_list()` |
| 服务管理 | 系统服务状态 | `get_service_list()` |
| 用户管理 | 用户列表、登录历史 | `get_users()`, `get_login_history()` |
| 软件管理 | APT源、已安装包 | `get_apt_sources()`, `get_installed_packages()` |
| 日志管理 | 系统日志、日志清理 | `get_system_logs()`, `cleanup_logs()` |
| 安全设置 | Kysec安全中心控制 | `get_kysec_status()`, `set_kysec()` |
| 命令终端 | 系统命令执行 | `execute_command()` |

### 3.2 功能详细说明

#### 3.2.1 系统信息监控

**功能说明**：获取系统基本信息，包括主机名、操作系统版本、内核版本、系统架构、运行时间等。

**数据结构**：
```json
{
  "status": "success",
  "data": {
    "hostname": "kylin-pc",
    "os_version": "Kylin Linux Advanced Server V10 SP1",
    "kernel": "5.4.0-70-generic",
    "architecture": "aarch64",
    "uptime": "up 2 hours, 30 minutes",
    "boot_time": "2024-01-15 09:00:00",
    "current_time": "2024-01-15 11:30:00",
    "platform": "Linux"
  }
}
```

#### 3.2.2 CPU监控

**功能说明**：获取CPU详细信息，包括物理核心数、逻辑核心数、CPU频率、使用率等。

**实现方式**：
- 使用 `psutil.cpu_count()` 获取核心数
- 使用 `psutil.cpu_percent()` 获取使用率
- 使用 `psutil.cpu_freq()` 获取频率信息

#### 3.2.3 内存监控

**功能说明**：获取内存使用情况，包括总内存、可用内存、已用内存、Swap分区等。

#### 3.2.4 磁盘监控

**功能说明**：获取磁盘分区信息，包括挂载点、文件系统类型、总容量、使用量等。

#### 3.2.5 网络管理

**功能说明**：获取网络接口信息、IP地址配置、网络连接状态，并支持重启网络服务。

#### 3.2.6 进程管理

**功能说明**：获取系统进程列表，支持按CPU使用率排序，显示TOP进程。

#### 3.2.7 服务管理

**功能说明**：获取系统服务状态列表。

#### 3.2.8 用户管理

**功能说明**：获取系统用户列表和登录历史记录。

#### 3.2.9 软件管理

**功能说明**：管理APT软件源和已安装软件包。

#### 3.2.10 日志管理

**功能说明**：查看系统日志并支持日志清理。

#### 3.2.11 安全设置

**功能说明**：管理Kysec安全中心状态。

#### 3.2.12 命令终端

**功能说明**：支持执行系统命令（带安全过滤）。

**安全特性**：
- 禁止危险命令：`rm -rf`, `mkfs`, `dd if=`, fork炸弹等
- 命令超时控制（30秒）

---

## 四、架构重构

### 4.1 重构背景

原项目采用传统的 Client-Server 架构，需要分别部署服务端和客户端组件，存在以下问题：

1. **部署复杂**：需要先启动服务端，再启动客户端
2. **网络依赖**：客户端需要连接服务端才能工作
3. **资源浪费**：需要两个独立进程运行
4. **维护成本高**：需要维护两套代码

### 4.2 重构优势

| 对比项 | 重构前 | 重构后 |
| :--- | :--- | :--- |
| 部署复杂度 | 高（需启动服务端） | 低（单应用程序） |
| 网络依赖 | 必须 | 无 |
| 进程数量 | 2个 | 1个 |
| 启动方式 | 先服务端后客户端 | 直接运行 |
| 代码维护 | 两套代码 | 一套代码 |
| 性能 | 网络开销 | 本地调用 |

### 4.3 新增文件

| 文件 | 说明 |
| :--- | :--- |
| `core/__init__.py` | 核心模块初始化 |
| `core/commands.py` | 核心命令实现 |
| `core/local_client.py` | 本地客户端代理 |
| `ui/__init__.py` | UI模块初始化 |
| `ui/main_window.py` | 整合后的主窗口 |
| `kylintools.py` | 整合版主入口 |
| `validate.py` | 功能验证脚本 |

---

## 五、部署与安装

### 5.1 依赖要求

| 依赖 | 版本 | 说明 |
| :--- | :--- | :--- |
| Python | >= 3.6 | 运行环境 |
| PyQt5 | >= 5.15.0 | GUI框架 |
| psutil | 最新 | 系统监控库 |

### 5.2 DEB打包

**构建命令**：
```bash
# 构建amd64架构包
bash build-deb.sh amd64

# 构建arm64架构包
bash build-deb.sh arm64

# 构建通用包
bash build-deb.sh all
```

**包结构**：
```
kylin-system-tools_1.0.0_amd64.deb
├── DEBIAN/
│   ├── control        # 包元信息
│   ├── postinst       # 安装后脚本
│   ├── prerm          # 卸载前脚本
│   ├── postrm         # 卸载后脚本
│   └── conffiles      # 配置文件列表
├── etc/kylin-system-tools/config.json
├── usr/
│   ├── bin/kylintools          # 启动脚本
│   └── share/kylin-system-tools/  # Python源代码
│       ├── core/
│       ├── ui/
│       └── kylintools.py
└── usr/share/applications/kylin-system-tools.desktop
```

### 5.3 安装与卸载

```bash
# 安装
sudo dpkg -i kylin-system-tools_1.0.0_amd64.deb
sudo apt-get install -f

# 卸载（保留配置）
sudo dpkg -r kylin-system-tools

# 完全卸载（删除配置）
sudo dpkg -P kylin-system-tools
```

---

## 六、使用说明

### 6.1 启动方式

**命令行启动**：
```bash
# 直接运行主程序
kylintools
```

**桌面快捷方式**：
- 通过应用菜单启动"银河麒麟运维管理工具"

### 6.2 功能界面

整合后的应用包含以下功能模块：

1. **系统信息** - 系统基本信息概览和实时统计
2. **系统状态** - CPU、内存、进程、服务的详细监控
3. **用户管理** - 用户列表和登录历史
4. **网络设置** - 网络信息和网络服务控制
5. **软件管理** - APT源配置和已安装软件包
6. **日志管理** - 系统日志查看和清理
7. **安全设置** - Kysec安全中心控制
8. **命令终端** - 系统命令执行

---

## 七、安全考虑

### 7.1 命令过滤

对危险命令进行过滤：
```python
dangerous_cmds = ['rm -rf', 'mkfs', 'dd if=', ':(){:|:&};:']
for dangerous in dangerous_cmds:
    if dangerous in cmd:
        return {'status': 'error', 'message': 'Command not allowed'}
```

### 7.2 权限控制

- 敏感操作（如网络重启、日志清理）需要root权限
- 建议以普通用户身份运行，仅在需要时使用sudo

---

## 八、技术特点

| 特性 | 说明 |
| :--- | :--- |
| **单应用架构** | 无需服务端，独立运行 |
| **跨平台** | 支持ARM/AMD64架构 |
| **异步加载** | 使用QThread实现后台数据加载 |
| **安全过滤** | 内置危险命令过滤机制 |
| **图形界面** | 基于PyQt5的友好UI |
| **兼容性** | 保留旧版代码，支持平滑过渡 |

---

## 九、版本历史

| 版本 | 日期 | 更新内容 |
| :--- | :--- | :--- |
| v1.0.0 | 2024 | 初始版本，包含基础运维功能 |
| v1.1.0 | 2024 | 架构重构，整合为单一应用程序 |

---

**文档版本**: v1.1.0  
**生成日期**: 2024年  
**适用平台**: 银河麒麟桌面操作系统 V10 SP1